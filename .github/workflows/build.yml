name: test

on: push
jobs:
  build-node:
    first-job:
      runs-on: ubuntu-latest
      container: node:14
      steps:
        - run: node --version
        - run: npm --version
        - run: actions/checkout@v3
        - run: npm install
        - run: node app.js
      